


int playerlist::addplayers(node *& head, char *new_players)
{
	if(!head)
	{
		if(head[index] == 0)
		{
			head[index] = new 
			++index
		}
		if(index < size)
		{
			

		}
		else return index = 0;
	}

	return addplayers(head->getnext(), new_players);
}

